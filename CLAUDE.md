# 코딩 행동 지침 (Karpathy Guidelines)

이 프로젝트(`src/`, 설정 파일 등 일반 코드 작업)에서 코드를 작성・수정・리팩터링할 때 아래 원칙을 따른다. 기사 발행 파이프라인(`.claude/agents/*`)은 각 에이전트 파일에 별도 지침이 있으므로 그쪽이 우선한다.

## 1. 코딩 전에 생각하기

- 가정을 세웠으면 명시적으로 말한다. 확신이 없으면 묻는다.
- 해석이 여러 갈래면 하나를 임의로 고르지 말고 선택지를 제시한다.
- 더 단순한 방법이 있으면 그것을 말한다. 필요하면 사용자 요청에 반박한다.
- 불명확하면 멈추고, 무엇이 불명확한지 짚은 뒤 질문한다.

## 2. 단순함 우선

- 문제를 해결하는 최소한의 코드만 작성한다. 추측성 코드는 넣지 않는다.
- 요청받지 않은 기능을 넣지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 넣지 않는다.
- 200줄로 짰는데 50줄이면 되는 게 보이면 다시 짠다.
- 기준: "시니어 엔지니어가 보면 과하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적으로 수정하기

- 반드시 고쳐야 하는 부분만 건드린다. 내가 어지른 것만 치운다.
- 기존 코드를 고칠 때 인접한 코드・주석・포맷팅을 "개선"하지 않는다.
- 깨지지 않은 것을 리팩터링하지 않는다.
- 내 스타일이 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 지우지 않는다.
- 내 변경으로 인해 생긴 고아(사용하지 않게 된 import/변수/함수)는 제거한다.
- 내 변경이 원인이 아닌 기존 죽은 코드는 요청 없이 지우지 않는다.
- 기준: 변경한 모든 줄이 사용자 요청과 직접 연결되어야 한다.

## 4. 목표 중심 실행

- 작업을 검증 가능한 목표로 바꾼다.
  - "검증 추가해줘" → "잘못된 입력에 대한 테스트를 작성하고 통과시킨다"
  - "버그 고쳐줘" → "버그를 재현하는 테스트를 작성하고 통과시킨다"
  - "X 리팩터링해줘" → "리팩터링 전후로 테스트가 통과하는지 확인한다"
- 여러 단계 작업은 간단한 계획을 먼저 말한다:
  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  3. [단계] → 검증: [확인 방법]
  ```
- 성공 기준이 약하면("동작하게 해줘") 계속 되물어야 한다. 성공 기준이 강하면 스스로 반복해 검증할 수 있다.
