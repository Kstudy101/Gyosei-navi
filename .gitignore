node_modules/
.next/
.cache/
infra/changedetection/.env
# 통계 데이터는 재취득 가능. 一次情報 원문(data/sources)의 .txt 추출본은 커밋한다
data/stats/
# PDF는 이 PC 정책(Azure Information Protection)으로 저장 직후 자동 암호화되어
# 커밋해도 「보호된 문서」 껍데기만 남는다. 정본은 .txt 추출본이고,
# 재취득 URL은 각 폴더의 README.md에 있다 (2026-08-19)
data/**/*.pdf
# 자동 생성 OG 이미지 (npm run og:generate — CI가 빌드 전에 생성, Actions 캐시로 증분화)
public/og/auto/
next-env.d.ts
tsconfig.tsbuildinfo
gyosei-portal-scaffold.zip
out/
.env*
!.env.example
.DS_Store
*.log
.vercel
.contentlayer

# MCP 도구 산출물 (로컬 전용)
.serena/

# 세션 작업분 로컬 스냅샷 (실제 코드는 각 원래 경로가 정본 — 여기는 사본이라 추적 안 함)
workspace/

# 로컬 스크래치/도구 설정 (세션별, 원본 아님)
.tmp/
.codex/
.agents/
