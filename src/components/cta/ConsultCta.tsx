/**
 * 相談予約 CTA — Phase C（開業後）専用コンポーネント。
 *
 * ★資格取得・行政書士登録までは絶対に有効化しないこと（行政書士法19条 / docs/06）。
 * 開業後にこのコンポーネントの実装を差し替えるだけで、全記事の CTA が
 * 「相談予約」に切り替わる設計（docs/02 設計原則5）。
 */
export function ConsultCta() {
  return null;
}
